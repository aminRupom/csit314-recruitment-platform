# Database

SQL schemas, migrations, and seed data for the recruitment platform.

Django manages most migrations automatically (`python manage.py makemigrations`).
This folder is for:

- Initial schema design (`schema.sql`)
- Manual SQL scripts (e.g., complex views, stored procedures)
- Seed data for development/testing (`seed_data.sql`)
- ER diagrams (`er-diagram.png`)
